# Q11 fixed visible-crossing execution re-check — frozen pre-fit protocol

Q11 reuses predictions from the final B1 cheap baseline and S0 GRU three-seed mean under two explicitly distinct research populations: (a) within-stock Q8 at the April-selected global context on the common context128-eligible cohort; (b) strict source-only Q10 transfer, whose context is selected using only the other four stocks' April Q8 scores per held-out fold. The two populations are labeled separately. Q11 does not refit, retune, recalibrate or choose a trading threshold from retrospective outcomes.

On each frozen original-event h20 prediction row, apply the already fixed **strict `abs(prediction_bps)>1`** decision at event t. For execution delay d=0,1,5 original messages, take visible best ask for a predicted buy and bid for a predicted sell at t+d; liquidate at the opposite visible best quote at the same original event t+20. The exit event stays t+20 for all delays. Require exact event-index and segment matches at entry and exit, finite valid crossed-book quotes, and the same base opportunity population for all three delays and all four predictor series. Report denominator and exclusions before model selection; model-specific selected coverage follows the same denominator. Use one share per selected opportunity, no fee, queue, impact, inventory, participation, or actual-fill assumption.

The development cache retains causal feature partitions but not most original snapshot partitions. Reconstruct each needed evaluation day from the CC BY 4.0 WSELOB-2017 V1 source file, verify its frozen full-file SHA-256, and require byte-identical snapshot Parquet SHA-256 against the frozen cache manifest before crossing. The reconstruction is private; raw files and row-level quotes remain outside Git. This changes no prediction, sample, threshold, or outcome rule.

For each stock/day/mode/arm/delay report common opportunities, selected opportunities, coverage, gross midpoint move, entry and exit half spreads, visible crossed markout, and undefined reasons. Aggregate equal stock/day with a separate pooled descriptive view; preserve all negative and null results. The reported crossing identity must hold numerically for every eligible selected row. Also report the baseline/GRU both-selected subset within each mode, without replacing the main selected-population comparison. Delay d>0 changes the observed entry quote and midpoint; the exit stays fixed at t+20.

This remains a research diagnostic on already exposed 2017 dates. It is not realized PnL or proof of fills. The old FQ3 zero-delay result remains published and is not overwritten. Freeze this protocol only after Q8/Q10 checkpoints and latest reviewer-control readback, before computing Q11 outcomes.

## Parent result identity

Q8 aggregate SHA-256 `c76e14660e517658c6e1b21f355d4f9b3467eefd8a5cb069497c2fe3362d3fcc`; Q10 aggregate SHA-256 `cfd6202b426bcc0dd21f2cf7593fb9f1df2baaf7ff5d81f8f5ea239ed04c7957`. These parent summaries and all five source files are checked before Q11 execution. The Q11 threshold and timing draft was written before opening the completed Q10 fold results; no Q10 sign or magnitude was used to revise it.
